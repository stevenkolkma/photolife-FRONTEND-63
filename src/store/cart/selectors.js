export const selectCartItems = (state) => state.cart.cartItems;
export const selectTotalPrice = (state) => state.cart.cartTotal;
export const selectQuantity = (state) => state.cart.cartItems.quantity;
